# 西游记取经路线地图可视化

## 项目目标
基于《西游记》文本内容，创建一个可交互的唐僧取经路线地图，展示唐僧师徒四人从长安到灵山的完整旅程。

## 技术栈
- HTML5 + CSS3 + JavaScript
- D3.js 用于数据可视化
- SVG 绘制路线和节点
- 古典/水墨卷轴风格界面（深色主题）

## 功能需求
1. 提取并展示取经路线的主要地点
2. 显示每个地点遇到的妖怪
3. 展示关键事件概要
4. 路线可交互（点击查看详情）
5. 不同类型妖怪的视觉区分
6. 古典风格的UI设计

## 文件结构
- index.html - 主页面
- css/style.css - 样式文件
- js/data.js - 取经数据
- js/main.js - 主逻辑
- js/tooltip.js - 提示框组件

## 长周期任务工作约定

### 每次开始必须执行
1. 确认当前目录与 git 状态（`git status -sb`）。
2. 阅读 `progress.md` 和 `tasks.md`。
3. 从 `tasks.md` 找出**唯一最高优先级且状态为 TODO 的任务**，作为本轮唯一主任务。
4. 若任务不明确或存在歧义，先输出简短执行计划，等待确认后再动手。

### 执行铁律
1. **一次只做一件事**：禁止并行处理多个功能。
2. **小步修改**：优先最小改动，禁止无明确需求的大范围重构。
3. **复用现有模式**：代码风格、命名与现有代码保持一致。
4. **高风险操作必须先说明**：新增依赖、删文件、改配置前，必须说明风险并等待确认。
5. **禁止顺手扩展**：额外优化点只能记录到 tasks.md / progress.md，本轮不得处理。

### 验证铁律
每次代码修改后必须执行至少一类验证：
- JS 语法检查（`node -c`）
- CSS 括号平衡检查
- main.js 引用的 DOM id 与 index.html 匹配检查
- 数据完整性 / 边界检查（node 脚本）

验证失败时：停止宣称完成，优先修复本轮引入的问题；连续失败超过 2 次必须停止并在输出中说明阻塞点。禁止在验证失败状态下标记任务为 DONE。

### 完成定义（必须全部满足）
1. 代码已落盘。
2. 验证已通过。
3. `progress.md` 已更新。
4. `tasks.md` 中对应任务状态已更新。
5. 输出中已说明：改了什么、怎么验证、下一步是什么。

### 文件维护规则
- `progress.md`：每轮结束**必须**更新，只写事实。
- `tasks.md`：任务状态变化时**必须**更新，同一时间只允许一个任务处于 DOING。
- 禁止删除历史完成记录。
- 每完成一轮，用 git 提交（tracking 文件 + 相关源码）。

### 输出格式（强制）
每轮结束按以下格式输出：

### 本轮完成
- 任务 ID 与名称 / 修改的文件列表 / 核心实现说明

### 验证结果
- 执行的命令 / 结果（成功/失败） / 失败原因（如有）

### 文件更新
- progress.md 已更新：是 / 否
- tasks.md 已更新：是 / 否

### 下一步
- 下一个最高优先级任务 / 是否存在阻塞或需人工确认的问题

### 上下文建议
- 是否建议 /clear：是 / 否
- 提示用户的话术（任务完成后建议清空上下文）
